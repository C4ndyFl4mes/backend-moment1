<!DOCTYPE html>
<html lang="sv">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="stylesheet" href="/styles/form.css">
    <title>Uppdatera kurs</title>
</head>

<body>
    <header>
        <div class="top">
            <p>Server-baserad webbutveckling Moment 1 - Isaac Svedin</p>
        </div>
        <nav class="menu">
            <ul>
                <li>
                    <a href="/">Kurser</a>
                </li>
                <li>
                    <a href="/courseform">Ny kurs</a>
                </li>
                <li>
                    <a href="/about">Om</a>
                </li>
            </ul>
        </nav>
    </header>
    <main>
        <h1>Kursformulär</h1>
        <form method="post" action="/update">
            <fieldset>
                <legend>Uppdatera kurs</legend>
                <div class="inputs">
                    <input type="number" name="courseid" value="<%= formData.id || '' %>" style="display: none;">
                    <label>
                        <span>Kurskod:</span>
                        <input type="text" name="coursecode" id="coursecode" value="<%= formData.code || '' %>">
                    </label>
                    <label>
                        <span>Kursnamn:</span>
                        <input type="text" name="coursename" id="coursename" value="<%= formData.name || '' %>">
                    </label>
                    <label>
                        <span>Progression:</span>
                        <input type="text" name="progression" id="progression"
                            value="<%= formData.progression || '' %>">
                    </label>
                    <label>
                        <span>Länk:</span>
                        <input type="url" name="courseurl" id="courseurl" value="<%= formData.syllabus || '' %>">
                    </label>
                </div>
                <div class="errors">
                    <% if (errors.length> 0) { %>
                        <% errors.forEach(error=> { %>
                            <article>
                                <h2>
                                    <%= error.name %>
                                </h2>
                                <p>
                                    <%= error.message %>
                                </p>
                            </article>
                            <% }); %>
                                <% }%>
                </div>
                <label class="label-submit">
                    <span>Vill du uppdatera den här kursen?</span>
                    <input type="submit" name="submit" id="submit">
                </label>
            </fieldset>
        </form>
    </main>
</body>

</html>